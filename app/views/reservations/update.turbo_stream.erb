<%= turbo_stream.update("#{dom_id(@active_reservation)}_row",
                          locals: { reservation: @active_reservation },
                          partial: 'reservations/row') %>

<%= turbo_stream.remove('active_reservation_badge') %>
